[alias]
review = "insta test --test-runner nextest --review --unreferenced delete"
x = "run --release --quiet -p xtask"
codegen = "x codegen"
ub = "x miri"
check-tco = "x check-tco"
sort-imports = "x sort-imports"
cli = "run --release --quiet -p hebi4-cli --"
playground = "x playground"
