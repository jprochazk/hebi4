//module
// Iteration over nested lists
let list = [[1, 2], [3, 4], [5, 6]]
let iter = list_iter(list)

assert_eq(iter(), [1, 2])
assert_eq(iter(), [3, 4])
assert_eq(iter(), [5, 6])
assert_eq(iter(), nil)
