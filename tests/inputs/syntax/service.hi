//parse

let svc = http.service()

svc.GET("/hello/:name", fn(req, res) {
  res.status(200)
  res.header("Content-Type", "text/plain")
  res.body(
    fmt("Hello, {name}!", { name: req.params.name })
  )
})

http.serve(svc)

